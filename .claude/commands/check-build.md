# Check Build

Run build checks to ensure the project compiles correctly.

## Steps

1. Run TypeScript type checking: `yarn typecheck`
2. Run linting: `yarn lint`
3. If there are errors, analyze them and suggest fixes
4. Report a summary of any issues found

Focus on newly modified files first (check `git status` and `git diff` for context).
